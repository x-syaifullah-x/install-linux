# SETUP PIPEWIRE

### INSTALL WIREPLUMBER
```bash
apt install --no-install-suggests --no-install-recommends wireplumber
```

libasound2 libasound2-data libasyncns0 libavahi-client3 libavahi-common-data libavahi-common3 libdbus-1-3 libflac12 libglib2.0-0 liblilv-0-0 liblua5.3-0 libmp3lame0 libmpg123-0 libogg0 libopus0 libpipewire-0.3-0 libpipewire-0.3-modules libpulse0 libreadline8 libserd-0-0 libsndfile1 libsord-0-0 libspa-0.2-modules libsratom-0-0 libvorbis0a libvorbisenc2 libwebrtc-audio-processing1 libwireplumber-0.4-0 libx11-6 libx11-data libx11-xcb1 libxau6 libxcb1 libxdmcp6 pipewire pipewire-bin readline-common wireplumber

### INSTALL PIPEWIRE-PULSE
```bash
apt install --no-install-suggests --no-install-recommends pipewire-pulse
```

### INSTALL PIPEWIRE-ALSA
```bash
apt install --no-install-suggests --no-install-recommends pipewire-alsa
```

### INSTALL PIPEWIRE-JACK
```bash
apt install --no-install-suggests --no-install-recommends libspa-0.2-jack pipewire-jack
```
- **Setup Configuration**
  ```bash
  ln -sf /usr/share/doc/pipewire/examples/ld.so.conf.d/pipewire-jack-*.conf /etc/ld.so.conf.d  && ldconfig
  ```

- **Test Pipewire Jack**

  - **Install jack-tools**
    ```bash
    apt install --no-install-suggests --no-install-recommends jack-tools
    ```

  - **Running jack_simple_client**
    ```bash
    jack_simple_client
    ```
---

### INSTALL PIPEWIRE-BLUETOOTH
```bash
apt install --no-install-suggests --no-install-recommends libspa-0.2-bluetooth
```

### INSTALL RTKIT
```bash
apt install --no-install-suggests --no-install-recommends rtkit
```

### INSTALL XDG-DESKTOP-PORTAL-GTK
```bash
apt install --no-install-suggests --no-install-recommends xdg-desktop-portal-gtk
```
```
xdg-desktop-portal xdg-desktop-portal-gtk
```
---